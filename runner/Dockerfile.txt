
# Base runners image
FROM n8nio/runners:latest

# Use root only for setup
USER root

# Create the virtual environment for n8n Python integrations
RUN python3 -m venv /opt/n8n-python-venv \
    && chown -R node:node /opt/n8n-python-venv

# Drop privileges and run as the standard non-root user
USER node
